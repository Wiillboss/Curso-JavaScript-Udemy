const a = 7
let b = 3

b += a // é a mesma coisa que (b = b + a)
console.log(b)

b -= 4 // é a mesma coisa que (b = b - a)
console.log(b)

b *= 2 // é a mesma coisa que (b = b * a)
console.log(b)

b /= 2 // é a mesma coisa que (b = b / a)
console.log(b)

b %= 2 // é a mesma coisa que (b = b % a) ... o modulo (%)vé o resto da divisão
console.log(b)