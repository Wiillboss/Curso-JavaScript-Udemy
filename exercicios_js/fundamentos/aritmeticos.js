const [a, b, c, d] = [3, 5, 1, 15]

const soma = a + b + c + d
const subtracao = d - b
const multiplicacao = a * b
const divisao = d /a
const modulo = a % 2

console.log(`A soma é igual: ${soma}.
A subtração é igual: ${subtracao}.
A multiplicação é igual: ${multiplicacao}.
A divisão é igual: ${divisao}.
O modudlo é igual: ${modulo}`
)